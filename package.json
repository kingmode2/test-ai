{
  "name": "test-ai",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "dev:lan": "vite --host 0.0.0.0",
    "build": "tsc && vite build && node scripts/copy-json.mjs",
    "preview": "vite preview",
    "preview:lan": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
